// Company (Dlužník) identity used in generated contracts.
// Keep the account number here only — the templates import it so the three
// contract renderers (PDF, HTML proposal, HTML contract) can never drift apart.

export const COMPANY_BANK_ACCOUNT = "284736043/0600";
